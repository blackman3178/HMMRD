{{#each drinks}}
<div class="card" >
  <div class="card header">
    <a class="card-body" href ="/reviews/{{id}}">{{drink_name}}</a>
  </div>
  <div class="card-body">
    {{#if image}}
    <img src="{{image}}" alt="Image of {{drink_name}}">
    {{else}}
    <img src="../public/images/hmmrdlight.png" alt="Image of {{drink_name}}">
    {{/if}}
    <p>
      {{review}}
    </p>
  </div>
</div>
{{/each}}